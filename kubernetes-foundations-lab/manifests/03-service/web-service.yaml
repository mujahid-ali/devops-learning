# Lab 05. The selector must match the Deployment's Pod-template label exactly.
apiVersion: v1
kind: Service
metadata:
  name: web
  namespace: playground
spec:
  type: NodePort
  selector:
    app: web
  ports:
    - name: http
      port: 80
      targetPort: http
      nodePort: 30080
