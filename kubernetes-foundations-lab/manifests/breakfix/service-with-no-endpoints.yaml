# Do not apply in a different cluster. This intentionally uses a selector that matches no Pods.
apiVersion: v1
kind: Service
metadata:
  name: broken-web
  namespace: playground
spec:
  selector:
    app: does-not-exist
  ports:
    - name: http
      port: 80
      targetPort: http
